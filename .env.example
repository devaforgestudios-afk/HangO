# HangO Environment Configuration
# Copy this file to .env and update the values

# Server Configuration
PORT=3000
NODE_ENV=development

# Domain Configuration (Update this with your actual domain)
# For development: http://localhost:3000
# For production: https://yourdomain.com
DOMAIN=http://localhost:3000

# Session Configuration
SESSION_SECRET=your-super-secret-session-key-change-in-production

# Email Service Configuration (Optional - for email verification)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@yourdomain.com

# Airtable Configuration (Optional - for persistent storage)
AIRTABLE_API_KEY=your-airtable-api-key
AIRTABLE_BASE_ID=your-airtable-base-id

# OAuth Configuration
# You can use the same Google credentials for both, or separate them for better control

# Google OAuth (For social login - optional)
GOOGLE_CLIENT_ID=your-google-oauth-client-id
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret

# Google Calendar API (For scheduling meetings - recommended to use separate credentials)
# Get these from https://console.developers.google.com/
# Create a separate OAuth 2.0 client for calendar-only access
GOOGLE_CALENDAR_CLIENT_ID=your-calendar-client-id
GOOGLE_CALENDAR_CLIENT_SECRET=your-calendar-client-secret

# GitHub OAuth (Optional - for GitHub social login)
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# TURN Server Configuration (Optional - for better WebRTC connectivity)
# TURN_SERVER_URL=turn:your-turn-server.com:3478
# TURN_USERNAME=your-turn-username  
# TURN_CREDENTIAL=your-turn-password